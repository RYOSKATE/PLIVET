language: node_js

node_js:
  - 'node'
  - '10.14.0'

dist: trusty
sudo: false  

matrix:
  fast_finish: true

branches:
  only: 
    - master
    - develop

cache:
  yarn: true
  directories:
    - node_modules

git:
  depth: 1
